function replacePlants (innerHTML) {
  const plants = document.getElementById('plants');
  plants.innerHTML = innerHTML;
}

if ('<%= @filter %>' === "[]") {
replacePlants("<%= j render 'plant', plants: policy_scope(Plant).all %>");
}
else {
replacePlants("<%= j render 'plant', plants: @plants %>");
}

