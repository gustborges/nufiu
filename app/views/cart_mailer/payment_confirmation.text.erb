Olá, <%= @user.name ? @user.name.titlecase : @user.email.split('@').first.titlecase %>! O seu pagamento no site da Nufiu foi confirmado.

Produtos adquiridos:
<% @cart.cart_plants.each do | cart_plant| %>
  - <%= cart_plant.amount %> x <%= cart_plant.plant.name %> = R$ <%= cart_plant.plant.price * cart_plant.amount %>
<% end %>
<% if @user.shipping.pick_up == false %>
  - Frete para <%= @user.shipping.suburb.name %> : R$ <%= @user.shipping.suburb.shipping_price %> 
  - Total: R$ <%= (@cart.amount / 100) + @user.shipping.suburb.shipping_price  %>
<% else %>
  - Total: R$ <%= (@cart.amount / 100) %>
<% end %>

Endereço de entrega:

- Endereço: <%= @user.shipping.address %>
- Complemento: <%= @user.shipping.address_complement %>
- Bairro: <%= @user.shipping.suburb.name %>
- CEP: <%= @user.shipping.zip_code %>
<% if @user.shipping.location_details %>
- Detalhes adicionais: <%= @user.shipping.location_details %>
<% end %>

Se você deseja adicionar algum detalhe, como escolher uma cor para os fios de sua kokedama, basta responder a esse e-mail com seus comentários.

Vamos selecionar suas plantinhas e preparar os arranjos com muito carinho. Você também deverá receber um contato nosso em breve para confirmarmos dados de entrega. 

Aliás, o prazo de entrega pode variar um pouquinho de acordo com a disponibilidade das espécies de plantas que você selecionou, mas vamos fazer o máximo para que elas cheguem ao seu endereço em até 7 dias corridos. 

Qualquer dúvida, estamos disponíveis e, mais uma vez, obrigada.
Malu Borges
Nufiu Jardinagem
www.nufiu.com.br
