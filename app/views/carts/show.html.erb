<% content_for :head do %>
    <%= stylesheet_link_tag 'pages/_cart', media: 'all', 'data-turbolinks-track': 'reload' %>
<% end %>

<div class="add-margins">
  <h1>🧺 Cesta de Compras</h1>
  <p>Antes de finalizar, confira os itens que já adicionou.</p>
</div>

<%= render partial: "cart" %>

  <div id="esvaziar">
      <%= link_to "🔥 Esvaziar carrinho", delete_all_cart_cart_plants_path(@cart), method: :delete %>
  </div>
  <div class="cart-disclaimer">
    <p><strong>Plantas podem variar</strong> de tamanhos, formatos, cores e desenhos. As imagens do site são próximas da realidade, mas ilustrativas.</p>
    <p><strong>Caso você tenha preferências nas cores dos fios</strong>, você pode enviar uma mensagem para a gente logo após a sua compra.</p>
  </div>

  <div class="cart-total">
    <p>Total: R$ <%= @cart.amount / 100 %></p> 
  </div>

  <div class="cart-buttons">
    <div id="pagar">
    <% if !@cart.cart_plants.empty? %>
      <%= link_to "Finalizar compras", new_cart_payment_path(cart_id: @cart.id), class: "green-button", id: "finalizar-compras" %>
      <% end %>
    </div>
    <div id="continuar-comprando">
      <%= link_to "👈 Continuar comprando", root_path %>
    </div>
  </div>
